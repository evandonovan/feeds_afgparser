A parser for the custom Footprint XML (fp:tagname) fields in 
an RSS feed that is generated by an AllForGood API query.

Requires Drupal 6, the Feeds module (latest version), and the
SimplePie PHP library.
